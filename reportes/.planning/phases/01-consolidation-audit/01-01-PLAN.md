---
phase: 01-consolidation-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-consolidation-audit/MERGE-DECISIONS.md
autonomous: true
requirements:
  - CORE-01

must_haves:
  truths:
    - "Every method in assessment_downloader.py across all 6 copies has a documented resolution (destination and base version)"
    - "Every method in assessment_analyzer.py across all distinct family copies has a documented resolution"
    - "Body-level diffs are confirmed for all methods marked 'body comparison needed' or 'likely identical' in the research"
    - "requirements.txt version conflicts are resolved with explicit pinning strategy and rationale"
    - "All env vars across all 5 projects are catalogued with naming conflicts resolved and a canonical name chosen"
    - "The main.py vs main_app.py entry point finding is formally documented"
  artifacts:
    - path: ".planning/phases/01-consolidation-audit/MERGE-DECISIONS.md"
      provides: "Complete merge decision document — the single deliverable for Phase 1"
      contains: "assessment_downloader method matrix, assessment_analyzer method matrix, requirements resolution, env var catalogue"
  key_links:
    - from: ".planning/phases/01-consolidation-audit/01-RESEARCH.md"
      to: ".planning/phases/01-consolidation-audit/MERGE-DECISIONS.md"
      via: "body diffs confirm or revise method decisions from research"
    - from: ".planning/phases/01-consolidation-audit/MERGE-DECISIONS.md"
      to: "Phase 2 planning"
      via: "Phase 2 implementer reads MERGE-DECISIONS.md before writing any core/ code"
---

<objective>
Produce MERGE-DECISIONS.md: a complete, self-contained merge decision document that gives the Phase 2 implementer explicit instructions for every method in assessment_downloader.py and assessment_analyzer.py — which version to use as base, which destination (core/ vs per-report), and how to handle every body-level divergence — plus resolved requirements.txt pinning and a consolidated env var catalogue.

Purpose: Phase 2 cannot begin until every method decision is explicit. The audit document IS the blocker gate. Nothing in core/ is written until this document exists and is complete.
Output: .planning/phases/01-consolidation-audit/MERGE-DECISIONS.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consolidation-audit/01-CONTEXT.md
@.planning/phases/01-consolidation-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm pending body diffs and resolve open questions</name>
  <files>.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md</files>
  <action>
Run shell diffs to confirm or revise the "body comparison needed" and "likely identical" method judgments from 01-RESEARCH.md. Record actual diff output (or "no output — identical") for each method. Then resolve the three open questions.

**Methods requiring body diff — run `diff -u` for each pair:**

Assessment Downloader (core-bound methods with divergence flags):
1. `_download_responses_incremental` — diff diagnosticos vs ensayos_generales versions (eg adds user timestamp logic)
2. `download_assessment_responses_incremental` — diff all three family versions (signature differs: diag/eg have `return_df`; aa lacks it)
3. `_download_responses_full` — diff diagnosticos vs ensayos_generales vs aa/rtd versions
4. `_download_assessment_responses_full` — diff diagnosticos vs ensayos_generales vs aa/rtd (marked "likely identical")
5. `_download_form_responses_full` — diff ensayos_generales vs assessment-analysis-project (CRITICAL: two versions exist; body must be compared before merge decision)
6. `_download_and_process_common` — diff diagnosticos vs ensayos_generales vs aa versions
7. `download_and_process_assessment` — diff all three families
8. `download_all_assessments` — diff all three families (marked "likely identical")
9. `delete_assessment_data` — diff all three families (marked "likely identical")
10. `filter_responses` — diff diagnosticos vs ensayos_generales vs aa (signature differs re: Union type)
11. `add_answer_columns_to_csv` — diff diagnosticos vs ensayos_generales vs aa

Assessment Analyzer (config-based family methods with divergence flags):
12. `_get_default_config` — diff diagnosticos vs diagnosticos_uim vs assessment-analysis-project (described as "differs significantly")
13. `analyze_assessment` — diff diagnosticos vs diagnosticos_uim vs assessment-analysis-project
14. `__init__` — diff all three config-based family copies

**Open questions to resolve:**

Q1: Was `_analyze_by_category_generic` intentionally removed from diagnosticos_uim?
- Read diagnosticos_uim/assessment_analyzer.py and confirm the method is absent
- Read diagnosticos_uim's test assessment types (F30M, B30M, Q30M) — determine if those types ever call category-based analysis
- Resolution: document as "use diagnosticos version as base; uim may have intentionally omitted this but the canonical core/ version must include it; uim plugin can ignore if its assessment types don't use category analysis"

Q2: Are the two `_download_form_responses_full` bodies equivalent?
- Covered by diff #5 above
- Resolution: if bodies differ, document exact divergence and which version to use as base (or how to merge)

Q3: Should `load_assessment_list_from_env` ever go to core/?
- Already resolved in research: stays per-report module
- Document the rationale formally in MERGE-DECISIONS.md

**Diff commands to run (using absolute paths):**
```bash
# Abbreviation map (use these paths):
# diag = /c/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/assessment_downloader.py
# uim  = /c/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos_uim/assessment_downloader.py
# eg   = /c/Users/Seba/Downloads/M30M/Data-analysis/reportes/ensayos_generales/assessment_downloader.py
# aa   = /c/Users/Seba/Downloads/M30M/Data-analysis/reportes/assessment-analysis-project/assessment_downloader.py

# Extract specific methods using grep -n to find line ranges, then diff the relevant sections
# Or extract full files and diff — the files are not large
diff -u diagnosticos/assessment_downloader.py ensayos_generales/assessment_downloader.py
diff -u diagnosticos/assessment_downloader.py assessment-analysis-project/assessment_downloader.py
diff -u ensayos_generales/assessment_downloader.py assessment-analysis-project/assessment_downloader.py

diff -u diagnosticos/assessment_analyzer.py diagnosticos_uim/assessment_analyzer.py
diff -u diagnosticos/assessment_analyzer.py assessment-analysis-project/assessment_analyzer.py
```

**Record results as:** For each method, one of:
- "Identical across all copies" — no diff output
- "Diverges: [brief description of difference]" — with the resolution

After diffs, all data is ready for Task 2.
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-consolidation-audit/MERGE-DECISIONS.md" &amp;&amp; echo "file exists"</automated>
  </verify>
  <done>Diff results are recorded (as notes or directly into MERGE-DECISIONS.md). Every "body comparison needed" and "likely identical" method from the research has a confirmed status. Three open questions have explicit resolutions.</done>
</task>

<task type="auto">
  <name>Task 2: Write MERGE-DECISIONS.md — the complete merge decision document</name>
  <files>.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md</files>
  <action>
Write the complete MERGE-DECISIONS.md file. This is the single deliverable for Phase 1. The Phase 2 implementer reads this document before writing a single line of core/ code.

**Document structure (follow exactly):**

```
# Merge Decisions: Unified Report Pipeline

**Produced:** 2026-02-28
**Status:** Final — all decisions explicit before Phase 2 begins
**Source:** 01-RESEARCH.md + body diffs from Task 1

## 1. assessment_downloader.py — Method Matrix

[Table: one row per method, columns: Method | diag | uim | eg | aa/rtd | Status | Destination]
Status values: IDENTICAL, DIVERGES, ABSENT

## 2. assessment_downloader.py — Resolution Decisions

[Table: one row per non-identical or partially-absent method]
Columns: Method | Situation | Resolution | Destination | Canonical Base | Notes

Resolution must be explicit enough that Phase 2 can implement without asking questions.

## 3. assessment_analyzer.py — Method Matrix

[Table: config-based family only]
Columns: Method | diag | uim | aa/rtd | Status

[Separate paragraph: ensayos_generales analyzer — explain it is architecturally separate, lives as reports/ensayos_generales/analyzer.py, zero methods promoted to core/]

## 4. assessment_analyzer.py — Resolution Decisions

[Same format as section 2]
Must include:
- _analyze_by_category_generic: why it is missing from uim and what the canonical core/ version includes
- analyze_assessment_from_csv: which version is the base (diagnosticos — has return_df + StorageClient)
- _get_default_config: which version and what the body diff found

## 5. requirements.txt — Conflict Resolution

[Version matrix table — copy from research, add Resolution and Rationale columns]
Must include explicit decisions on:
- Pinning style: exact pins (==X.Y.Z) — rationale: matches production complete_deployment/ environment
- pandas: adopt 2.2.2 (matches Cloud Run production) — document breaking changes risk
- weasyprint: adopt 66.0 (matches Cloud Run production)
- reportlab: adopt 4.4.3 (matches Cloud Run production)
- numpy: pin 1.26.4 as companion to pandas 2.2.2
- protobuf: include ==4.* with note (needed by diag/uim for Firestore/Cloud Tasks)
- functions-framework: include with note (Cloud Run webhook handler only — not needed for local batch runs)
- google-auth-oauthlib / google-auth-httplib2: include from aa family

[Final: paste the proposed unified requirements.txt content]

## 6. Environment Variables — Consolidation Catalogue

[Master variable table — copy from research]
Columns: Variable | diag | uim | eg | aa/rtd | shared | Canonical Name | Notes

Must include:
- GOOGLE_CLOUD_PROJECT vs GCP_PROJECT_ID: canonical name → GCP_PROJECT_ID (rationale: avoids collision with GCP-native runtime var)
- Assessment ID vars: grouped by report type (per-report env vars, not in core .env)
- Core shared vars vs per-report vars: clearly separated

[Final: paste the proposed .env.example content with comments grouping vars by scope]

## 7. Entry Points — Cloud Run vs Local

Must document:
- complete_deployment/main.py = Cloud Run webhook handler (functions_framework, webhook_handler function)
- complete_deployment/main_app.py = standalone batch runner (local)
- Parent-dir main.py = standalone batch runner (NOT deployed to Cloud Run)
- Confirmed by: deploy.sh --entry-point=webhook_handler

## 8. Core Boundary Rules (Summary)

Summary table of the locked boundary decisions from CONTEXT.md, for Phase 2 reference:
- Default rule: methods in only 1 project + report-domain-specific → per-report module
- Override: user/form infrastructure → core/ regardless of current distribution
- Explicitly confirmed core/ destinations: list the infrastructure methods
- Explicitly confirmed per-report destinations: _normalize_commune, _compare_emails, _process_email_columns, load_assessment_list_from_env

## 9. Open Questions — Resolved

List all three open questions from 01-RESEARCH.md with their final resolutions.
```

**Content guidelines:**
- Every method row must have an explicit Destination (core/ or reports/X/)
- Every divergence must have a named base version (e.g., "use diagnosticos/ as base")
- The proposed requirements.txt must be a complete, pasteable file — not a description
- The proposed .env.example must be a complete, pasteable file with section comments
- Do NOT write vague entries like "TBD" or "decide in Phase 2" — Phase 1's job is to eliminate all TBDs

**Naming conflict resolution (use these decisions):**
- `cleanup_incremental_files` vs `cleanup_temp_files`: use `cleanup_temp_files` as canonical name (per discretion — more general; incremental files ARE temp files)
- `GOOGLE_CLOUD_PROJECT` vs `GCP_PROJECT_ID`: use `GCP_PROJECT_ID` (per discretion — explicit, avoids GCP runtime collision)
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
content = open('.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md').read()
checks = [
    ('## 1.' in content, 'Section 1 downloader method matrix'),
    ('## 2.' in content, 'Section 2 downloader resolution'),
    ('## 3.' in content, 'Section 3 analyzer method matrix'),
    ('## 4.' in content, 'Section 4 analyzer resolution'),
    ('## 5.' in content, 'Section 5 requirements'),
    ('## 6.' in content, 'Section 6 env vars'),
    ('## 7.' in content, 'Section 7 entry points'),
    ('## 8.' in content, 'Section 8 core boundary rules'),
    ('cleanup_temp_files' in content, 'Canonical cleanup name resolved'),
    ('GCP_PROJECT_ID' in content, 'Canonical project ID var resolved'),
    ('TBD' not in content, 'No unresolved TBD entries'),
]
failures = [msg for ok, msg in checks if not ok]
if failures:
    print('FAIL:', failures); sys.exit(1)
print('PASS: all sections present, no TBDs')
"
    </automated>
  </verify>
  <done>MERGE-DECISIONS.md exists with all 9 sections complete. Every method has an explicit destination and base version. requirements.txt and .env.example candidates are complete and pasteable. No TBD entries remain. The document is self-contained enough for Phase 2 to begin without re-reading the source files.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. MERGE-DECISIONS.md exists at `.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md`
2. All 9 sections are present
3. No "TBD", "unclear", or "decide later" entries exist in the document
4. The proposed `requirements.txt` in Section 5 is a complete, pasteable file
5. The proposed `.env.example` in Section 6 is a complete, pasteable file with section comments
6. Every method in the downloader matrix has a Destination column value of either `core/` or `reports/<type>/`
7. Every method in the analyzer matrix has a Destination column value
8. The three open questions from 01-RESEARCH.md each have a final resolution documented in Section 9
</verification>

<success_criteria>
Phase 1 is complete when:
- MERGE-DECISIONS.md exists and passes the automated verify check
- A Phase 2 implementer can read MERGE-DECISIONS.md alone and know: (a) which version of every method to use as the canonical base, (b) where every method goes (core/ or per-report), (c) exactly what to put in requirements.txt and .env.example, (d) which file is the Cloud Run entry point for each deployed project
- No file in core/ has been created (this phase is audit-only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-consolidation-audit/01-01-SUMMARY.md` using the template at `.claude/get-shit-done/templates/summary.md`
</output>

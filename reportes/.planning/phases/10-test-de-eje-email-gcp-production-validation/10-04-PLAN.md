---
phase: 10-test-de-eje-email-gcp-production-validation
plan: 04
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - core/email_sender.py
  - core/runner.py
  - reports/test_de_eje/email_template.py
  - tests/test_runner.py
autonomous: true
requirements:
  - MAIL-01
must_haves:
  truths:
    - "Each report type can define its own email subject and body text without modifying shared runner or sender code."
    - "`test_de_eje` sends emails with a subject and body text distinct from the generic diagnostic defaults."
    - "Future plugins add email templates by dropping a new `email_template.py` into their plugin directory — zero changes to runner or sender."
    - "If a report type has no email template, a safe generic fallback is used (no crash, no silent wrong content)."
  artifacts:
    - path: reports/test_de_eje/email_template.py
      provides: test_de_eje-specific subject and body text definitions
      contains: "subject|body"
    - path: core/email_sender.py
      provides: send method accepting optional subject/body overrides per report type
      contains: "subject|body|send"
    - path: core/runner.py
      provides: per-report-type email template lookup passed into sender
      contains: "email_template|report_type"
    - path: tests/test_runner.py
      provides: executable assertions proving test_de_eje sends with correct subject/body
      contains: "test_de_eje|subject|body"
  key_links:
    - from: core/runner.py
      to: reports/test_de_eje/email_template.py
      via: runner loads template for report_type at send time using importlib or direct import
      pattern: "email_template|get_email_template|report_type"
    - from: core/runner.py
      to: core/email_sender.py
      via: runner passes resolved subject and body to sender call
      pattern: "subject=|body="
---

<objective>
Implement per-report-type email subject and body templates so each plugin sends distinct, contextually correct email content without modifying shared infrastructure.

Purpose: MAIL-01 requires one-event/one-email. Gap-closure decisions (CONTEXT.md `decisions_update`) now require each report type to send a distinct subject and body — a deferred feature that is now required in Phase 10.

Output: `reports/test_de_eje/email_template.py` defines the test_de_eje content; `core/email_sender.py` accepts subject/body parameters; `core/runner.py` resolves and passes the template; tests assert test_de_eje emails carry the correct subject/body and that the extension point works for future plugin types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-test-de-eje-email-gcp-production-validation/10-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From core/email_sender.py — current send method signature:
```python
def send_comprehensive_report_email(
    self,
    recipient_email: str,
    pdf_content: bytes,
    username: str,
    filename: str,
    drive_link: str = None,
    correlation_key: str = None,
) -> bool:
```
Current subject (line 75): `msg["Subject"] = f"Resultados de Diagnóstico"`
Current body (lines 80-88): hardcoded Spanish paragraph about diagnostic test.

From core/runner.py — how sender is called (lines 83-91):
```python
def _send_email(self, recipient, pdf_path, drive_link, correlation_key=None):
    sender = EmailSender()
    return sender.send_comprehensive_report_email(
        recipient_email=recipient,
        pdf_content=pdf_path.read_bytes(),
        username=recipient,
        filename=pdf_path.name,
        drive_link=drive_link,
        correlation_key=correlation_key,
    )
```
`self.report_type` is available on the runner instance.

From reports/__init__.py — REGISTRY keys:
`"diagnosticos"`, `"diagnosticos_uim"`, `"ensayos_generales"`, `"test_diagnostico"`, `"test_de_eje"`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_de_eje email template module</name>
  <files>reports/test_de_eje/email_template.py</files>
  <behavior>
    - Module exports two string constants: `SUBJECT` and `BODY`.
    - `SUBJECT` is a non-empty string specific to test_de_eje (not the generic "Resultados de Diagnóstico").
    - `BODY` is a non-empty string that mentions test de eje context (study plan, units, etc.) rather than the generic diagnostic text.
    - Importing the module twice returns the same constants (idempotent, no side effects).
  </behavior>
  <action>
    Create `reports/test_de_eje/email_template.py` with two module-level string constants:

    ```python
    SUBJECT = "Tu informe Test de Eje"
    BODY = """Hola,

Has completado tu Test de Eje correctamente. En el informe adjunto encontrarás:
1. Tu nivel de dominio por unidad
2. Un plan de estudio personalizado con las lecciones recomendadas
3. El detalle de tu rendimiento en cada pregunta

Cualquier consulta, estamos aquí para ayudarte.

Un abrazo a la distancia
"""
    ```

    The subject and body must be meaningfully different from the generic diagnostic email currently hardcoded in `core/email_sender.py` (subject: "Resultados de Diagnóstico"; body: about "test de diagnóstico"). This is the contract — exact wording can be adjusted as long as content is test_de_eje-specific and non-generic.
  </action>
  <verify>
    <automated>python -c "from reports.test_de_eje.email_template import SUBJECT, BODY; assert SUBJECT and BODY and 'Resultados de Diagnóstico' not in SUBJECT; print('OK')"</automated>
  </verify>
  <done>Template module exists with distinct SUBJECT and BODY constants importable as `from reports.test_de_eje.email_template import SUBJECT, BODY`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional subject/body parameters to EmailSender and runner template resolution</name>
  <files>core/email_sender.py, core/runner.py, tests/test_runner.py</files>
  <behavior>
    EmailSender.send_comprehensive_report_email:
    - New optional params `subject: str = None` and `body: str = None`.
    - When `subject` is provided and non-empty, `msg["Subject"]` uses the provided value; otherwise falls back to existing hardcoded default.
    - When `body` is provided and non-empty, the body text uses the provided value; otherwise falls back to existing hardcoded default.
    - `drive_link` appended to provided body using the same existing pattern.
    - No behavior change for callers that omit subject/body (backward compatible).

    PipelineRunner._send_email / run():
    - Attempt to import `reports.{report_type}.email_template` and read `SUBJECT`, `BODY`.
    - If import succeeds: pass as `subject=` and `body=` to sender.
    - If import fails (module not found): use None for both (sender falls back to defaults).
    - Template lookup must not raise — ImportError is caught and logged at DEBUG level.

    Tests in tests/test_runner.py:
    - Assert that a mock PipelineRunner for report_type="test_de_eje" passes a non-None, non-generic subject to the sender.
    - Assert that a mock runner for an unregistered/templateless type passes None (falls back gracefully).
    - Assert backward compatibility: existing calls without subject/body still work.
  </behavior>
  <action>
    1. In `core/email_sender.py`, update `send_comprehensive_report_email` signature to add `subject: str = None` and `body: str = None` parameters after `filename`. Use them to compose `msg["Subject"]` and the body text, keeping existing defaults when the params are None or empty. Preserve `drive_link` append logic in both paths.

    2. In `core/runner.py`, add a private helper `_get_email_template(self) -> tuple[Optional[str], Optional[str]]` that:
       - Tries `importlib.import_module(f"reports.{self.report_type}.email_template")`
       - Returns `(module.SUBJECT, module.BODY)` on success
       - Returns `(None, None)` on `ImportError` (logged at DEBUG)
       - Returns `(None, None)` on any other exception (logged at WARNING)

    3. In `core/runner.py`, update `_send_email` to call `_get_email_template()` and forward `subject=` and `body=` to `send_comprehensive_report_email`.

    4. In `tests/test_runner.py`, add tests:
       - `test_test_de_eje_sends_typed_email_subject`: mock EmailSender.send_comprehensive_report_email, instantiate PipelineRunner("test_de_eje"), call `_get_email_template()`, assert returned subject is not None and differs from "Resultados de Diagnóstico".
       - `test_unknown_report_type_email_template_falls_back`: call `_get_email_template()` on a PipelineRunner with report_type="nonexistent_type", assert returns `(None, None)`.
       - Do not break existing test_runner.py tests — only add new ones.

    Import to add to runner.py: `import importlib` (stdlib, no new dependency).
  </action>
  <verify>
    <automated>python -m pytest tests/test_runner.py -k "email_template or typed_email or falls_back" -q</automated>
  </verify>
  <done>EmailSender accepts subject/body overrides; runner resolves per-plugin templates; test_de_eje emails carry plugin-specific content; fallback to defaults is safe and tested.</done>
</task>

</tasks>

<verification>
1. Template module importable: `python -c "from reports.test_de_eje.email_template import SUBJECT, BODY; print(SUBJECT)"` — prints test_de_eje-specific subject.
2. Template resolution works end-to-end: `python -m pytest tests/test_runner.py -k "email" -q` — all email-related tests pass.
3. Full runner regression: `python -m pytest tests/test_runner.py -q` — no regressions.
4. Existing report types unaffected: `python -m pytest tests/ -q` — full suite still passes.
</verification>

<success_criteria>
- `reports/test_de_eje/email_template.py` exists with distinct SUBJECT and BODY.
- `EmailSender.send_comprehensive_report_email` accepts optional subject/body without breaking existing callers.
- `PipelineRunner` resolves per-report-type templates via importlib with safe fallback.
- New plugin email templates can be added by creating `reports/{type}/email_template.py` — zero changes required in runner or sender.
- All tests pass: `python -m pytest tests/test_runner.py -q`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-de-eje-email-gcp-production-validation/10-04-SUMMARY.md`
</output>

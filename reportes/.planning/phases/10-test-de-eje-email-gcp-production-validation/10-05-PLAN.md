---
phase: 10-test-de-eje-email-gcp-production-validation
plan: 05
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-02"
  - "10-03"
  - "10-04"
files_modified:
  - webhook_service.py
  - .planning/phases/10-test-de-eje-email-gcp-production-validation/10-VERIFICATION.md
autonomous: false
requirements:
  - MAIL-01
  - DEPL-02
user_setup:
  - service: gcp-cloud-run
    why: "Deploy updated codebase with test_de_eje plugin and all Phase 10 fixes"
    env_vars:
      - name: GOOGLE_CLOUD_PROJECT
        source: "GCP Console -> Project Info"
      - name: CLOUD_RUN_SERVICE_NAME
        source: "GCP Console -> Cloud Run -> service name used in Phase 5"
      - name: CLOUD_RUN_REGION
        source: "GCP Console -> Cloud Run -> region (e.g. us-central1)"
    dashboard_config:
      - task: "Confirm Cloud Run service exists from Phase 5 deployment"
        location: "GCP Console -> Cloud Run"
  - service: gcs-ids-xlsx
    why: "Production assessment mapping source must be ids.xlsx in configured GCS bucket"
    env_vars:
      - name: IDS_XLSX_GCS_PATH
        source: "GCP Console -> Cloud Storage -> bucket/path used as mapping source (format: gs://bucket/path/ids.xlsx)"
      - name: GCP_BUCKET_NAME
        source: "GCP Console -> Cloud Storage -> bucket name (only needed if IDS_XLSX_GCS_PATH is not gs:// prefixed)"
    dashboard_config:
      - task: "Upload local inputs/ids.xlsx to the GCS path configured in IDS_XLSX_GCS_PATH"
        location: "GCP Console -> Cloud Storage -> bucket browser, or use: gsutil cp inputs/ids.xlsx gs://BUCKET/PATH/ids.xlsx"
must_haves:
  truths:
    - "Production Cloud Run service contains the Phase 10 codebase including test_de_eje plugin, email template support, and all hardening fixes."
    - "`ids.xlsx` is present at the GCS path configured in `IDS_XLSX_GCS_PATH` and the production mapper resolves test_de_eje assessment IDs from it."
    - "Webhook diagnostic logs reference `IDS_XLSX_GCS_PATH` (not `IDS_XLSX_PATH`) so the GCS mapping source is unambiguously identifiable in production logs."
    - "At least one full production `test_de_eje` webhook run (route -> queue -> process -> email) succeeds with a unique correlation ID and evidence is attached to the phase verification matrix."
    - "All five Phase 10 roadmap success criteria have explicit PASS or FAIL entries with evidence in 10-VERIFICATION.md."
  artifacts:
    - path: webhook_service.py
      provides: ids_path diagnostic log context using IDS_XLSX_GCS_PATH env key instead of IDS_XLSX_PATH
      contains: "IDS_XLSX_GCS_PATH"
    - path: .planning/phases/10-test-de-eje-email-gcp-production-validation/10-VERIFICATION.md
      provides: updated evidence matrix with production run results attached
      contains: "PASS|FAIL|correlation_id|evidence"
  key_links:
    - from: webhook_service.py
      to: core/assessment_mapper.py
      via: ids_path log context must match the env key actually used by AssessmentMapper._resolve_gcs_target (IDS_XLSX_GCS_PATH)
      pattern: "IDS_XLSX_GCS_PATH"
    - from: .planning/phases/10-test-de-eje-email-gcp-production-validation/10-PRODUCTION-RUNBOOK.md
      to: .planning/phases/10-test-de-eje-email-gcp-production-validation/10-VERIFICATION.md
      via: production run evidence collected via runbook populates verification matrix rows
      pattern: "checkpoint|evidence|PASS"
---

<objective>
Deploy all Phase 10 code changes to GCP Cloud Run, upload `ids.xlsx` to the production GCS mapping path, fix the `ids_path` env-key mismatch in webhook diagnostics, and collect production evidence proving `test_de_eje` end-to-end execution.

Purpose: Phase 10 exit criteria require at least one full production webhook run (route -> queue -> process -> email) with evidence. GCP deployment, GCS file upload, and the env-key diagnostics fix are all required for trustworthy production validation.

Output: Updated Cloud Run service, `ids.xlsx` at GCS path, corrected `webhook_service.py` diagnostics, and a completed 10-VERIFICATION.md evidence matrix proving MAIL-01 and DEPL-02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-test-de-eje-email-gcp-production-validation/10-CONTEXT.md
@.planning/phases/10-test-de-eje-email-gcp-production-validation/10-VERIFICATION.md
@.planning/phases/10-test-de-eje-email-gcp-production-validation/10-PRODUCTION-RUNBOOK.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From webhook_service.py — the mismatch (lines 223-224 and 264-265):
```python
# Current (WRONG key in diagnostics):
ids_path=(os.getenv("IDS_XLSX_PATH") or "default"),

# Required fix — use the key that assessment_mapper.py actually reads:
ids_path=(os.getenv("IDS_XLSX_GCS_PATH") or os.getenv("IDS_XLSX_PATH") or "default"),
```
There are three call sites in handle_webhook that log `ids_path`: the rejected-route warning (~line 223), the queue insertion log (~line 264), and the counter increment log (~line 280). All three must be updated to the same expression.

From core/assessment_mapper.py — confirmed env key (line 109):
```python
configured = os.getenv("IDS_XLSX_GCS_PATH", "").strip()
```
The mapper uses `IDS_XLSX_GCS_PATH`. The diagnostic log must match this key.

Phase 5 deployment pattern (from project history): Cloud Run deployment used `gcloud run deploy` with source-based build. Use the same service name and region as Phase 5.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix ids_path env-key mismatch in webhook_service.py diagnostics</name>
  <files>webhook_service.py</files>
  <action>
    Fix the three `ids_path` diagnostic log entries in `handle_webhook()` so they read from `IDS_XLSX_GCS_PATH` (the key `AssessmentMapper` actually uses) before falling back to `IDS_XLSX_PATH`.

    In `handle_webhook()`, find all occurrences of:
    ```python
    ids_path=(os.getenv("IDS_XLSX_PATH") or "default"),
    ```
    Replace each with:
    ```python
    ids_path=(os.getenv("IDS_XLSX_GCS_PATH") or os.getenv("IDS_XLSX_PATH") or "default"),
    ```

    There are exactly three call sites (rejected-route warning, queue insertion log, counter increment log). Update all three. No other logic changes.

    Verify the fix resolves the VERIFICATION.md code gap: "IDS_XLSX_PATH for ids_path, but core/assessment_mapper.py uses IDS_XLSX_GCS_PATH".
  </action>
  <verify>
    <automated>python -c "import ast, pathlib; src = pathlib.Path('webhook_service.py').read_text(); assert 'IDS_XLSX_GCS_PATH' in src and src.count('IDS_XLSX_GCS_PATH') >= 3; print('env-key fix verified')"</automated>
  </verify>
  <done>`webhook_service.py` diagnostic logs reference `IDS_XLSX_GCS_PATH` as the primary ids_path key in all three handle_webhook log contexts. Code gap from VERIFICATION.md is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Phase 10 codebase to GCP Cloud Run and upload ids.xlsx to GCS</name>
  <files></files>
  <action>
    Execute two sequential deployment operations using CLI tools.

    **Step 2a — Deploy to Cloud Run:**
    Run the following from the project root (same pattern as Phase 5 deployment):
    ```bash
    gcloud run deploy $CLOUD_RUN_SERVICE_NAME \
      --source . \
      --region $CLOUD_RUN_REGION \
      --project $GOOGLE_CLOUD_PROJECT \
      --quiet
    ```
    Wait for deployment to complete. Note the service URL from the output.

    **Step 2b — Upload ids.xlsx to GCS:**
    Upload the local `inputs/ids.xlsx` to the production GCS path:
    ```bash
    gsutil cp inputs/ids.xlsx $IDS_XLSX_GCS_PATH
    ```
    Then confirm the file is present:
    ```bash
    gsutil ls $IDS_XLSX_GCS_PATH
    ```
    Expected: command returns one line with the GCS path.

    **Step 2c — Verify deployment contains test_de_eje:**
    Hit the /status endpoint of the deployed service and confirm `test_de_eje` appears in `report_types`:
    ```bash
    curl -s "$SERVICE_URL/status" | python -c "import sys, json; d=json.load(sys.stdin); assert 'test_de_eje' in d.get('report_types', {}), 'test_de_eje not in registry'; print('test_de_eje confirmed in deployed registry')"
    ```

    If any step fails, record the error output. Do not proceed to the checkpoint without completing deployment and GCS upload.
  </action>
  <verify>
    <automated>gsutil ls $IDS_XLSX_GCS_PATH</automated>
  </verify>
  <done>Cloud Run service is updated with Phase 10 code; `ids.xlsx` exists at `$IDS_XLSX_GCS_PATH`; /status endpoint confirms `test_de_eje` in deployed registry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - All Phase 10 code (cardinality hardening, Firestore observability, email templates, env-key fix) deployed to Cloud Run.
    - `ids.xlsx` uploaded to production GCS bucket at `IDS_XLSX_GCS_PATH`.
    - Production mapper now resolves test_de_eje IDs from GCS.
  </what-built>
  <how-to-verify>
    Execute the production validation run using the 10-PRODUCTION-RUNBOOK.md and the `scripts/phase10_validate_test_de_eje_webhook.ps1` script. Collect evidence for all five success criteria:

    1. **Trigger one real test_de_eje webhook event** (use a test student account or the webhook trigger procedure in the runbook). Capture the correlation ID from the webhook response.

    2. **Firestore check**: Confirm queue entry, counter increment, and batch state transition for `report_types/test_de_eje/...` using the script or GCP Console.

    3. **Email proof**: Confirm one email arrived in the recipient inbox with exactly one PDF attachment named `informe_{email}_*.pdf`. Screenshot or log the inbox entry with timestamp.

    4. **Log check**: In Cloud Run logs, search for the correlation ID and confirm:
       - `ids_path` log entry shows `IDS_XLSX_GCS_PATH` value (not empty/default)
       - `mapping_source=gcs` appears in route resolution log
       - `queue_inserted=True` and `counter_incremented=True` appear

    5. **ids.xlsx verification**: In the Cloud Run logs, confirm the mapper loaded from GCS (look for "Loaded ids.xlsx routes" with `mapping_source: gcs`).

    After collection, update `.planning/phases/10-test-de-eje-email-gcp-production-validation/10-VERIFICATION.md` with PASS/FAIL for each of the five roadmap success criteria, attaching evidence references (timestamps, log excerpts, screenshot paths).
  </how-to-verify>
  <resume-signal>
    Type "approved" when all five verification matrix rows have been filled in 10-VERIFICATION.md with PASS or documented FAIL with evidence. If any required checkpoint FAILS, describe the failure so a gap-closure plan can be issued.
  </resume-signal>
</task>

</tasks>

<verification>
1. `webhook_service.py` contains `IDS_XLSX_GCS_PATH` in ids_path diagnostic context (automated: grep confirms 3+ occurrences).
2. Cloud Run deployment succeeds: `gcloud run services describe $CLOUD_RUN_SERVICE_NAME --region $CLOUD_RUN_REGION` shows ACTIVE.
3. GCS file confirmed: `gsutil ls $IDS_XLSX_GCS_PATH` returns the file path.
4. /status endpoint confirms `test_de_eje` in `report_types`.
5. 10-VERIFICATION.md has explicit PASS/FAIL with evidence for all five Phase 10 success criteria.
6. Phase 10 is complete only if all five rows are PASS. Any FAIL keeps phase status incomplete.
</verification>

<success_criteria>
- Env-key mismatch in webhook_service.py is closed: `IDS_XLSX_GCS_PATH` is the primary key in all diagnostic log contexts.
- Cloud Run service is running Phase 10 codebase (all plans 01-04 changes included).
- `ids.xlsx` is present at `IDS_XLSX_GCS_PATH` in GCS and mapper loads from it in production.
- Production webhook run evidence (correlation_id, Firestore state, email with attachment, log excerpts) is attached to 10-VERIFICATION.md.
- Phase 10 success criteria 1-5 all have PASS status in the evidence matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-de-eje-email-gcp-production-validation/10-05-SUMMARY.md`
</output>

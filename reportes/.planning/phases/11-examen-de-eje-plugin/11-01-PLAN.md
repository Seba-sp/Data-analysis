---
phase: 11-examen-de-eje-plugin
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_examen_de_eje_phase11_logic.py
  - tests/test_examen_de_eje_phase11_data_contract.py
  - tests/test_examen_de_eje_phase11_render_contract.py
autonomous: true
requirements:
  - PLUG-02
  - DATA-02
must_haves:
  truths:
    - "Failing tests exist for PDU% threshold logic at boundary values (49.9%, 50%, 79.9%, 80%)"
    - "Failing tests exist for bank column validation (accepts pregunta/alternativa/unidad; rejects missing column)"
    - "Failing test exists for REGISTRY containing examen_de_eje key"
    - "Failing tests exist for analyze() returning per-unit list in bank row order"
    - "Failing tests exist for render() producing PDF with correct filename and dynamic table rows"
  artifacts:
    - path: "tests/test_examen_de_eje_phase11_logic.py"
      provides: "PDU% threshold + analyze unit-order unit tests"
    - path: "tests/test_examen_de_eje_phase11_data_contract.py"
      provides: "REGISTRY, bank column validation, mapping load unit tests"
    - path: "tests/test_examen_de_eje_phase11_render_contract.py"
      provides: "render() output path + PDF filename + dynamic table injection tests"
  key_links:
    - from: "tests/test_examen_de_eje_phase11_data_contract.py"
      to: "reports/examen_de_eje/generator.py"
      via: "import ExamenDeEjeGenerator"
      pattern: "from reports.examen_de_eje.generator import"
    - from: "tests/test_examen_de_eje_phase11_logic.py"
      to: "reports/examen_de_eje/generator.py"
      via: "import _assign_estado_recomendacion, UnitStats, ExamenPlan"
      pattern: "from reports.examen_de_eje.generator import"
---

<objective>
Write all failing tests for the examen_de_eje plugin before any implementation exists. Tests must fail (ImportError or AssertionError) at the end of this plan, confirming RED state.

Purpose: Establish the behavioral contract for ExamenDeEjeGenerator before writing a single line of implementation. Tests act as a precise spec for Plan 02.
Output: Three test files covering PDU% logic, data contract/REGISTRY, and render contract — all in RED state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-examen-de-eje-plugin/11-CONTEXT.md
@.planning/phases/11-examen-de-eje-plugin/11-RESEARCH.md

<interfaces>
<!-- Reference implementation: reports/test_de_eje/generator.py -->
<!-- Key test patterns from tests/test_test_de_eje_phase9_logic.py and tests/test_test_de_eje_phase9_data_contract.py -->

From tests/test_test_de_eje_phase9_data_contract.py (test structure pattern):
```python
from reports import REGISTRY
from reports.test_de_eje.generator import MappingRow, TestDeEjeGenerator

def _workdir(prefix: str) -> Path: ...
def _write_ids_xlsx(path: Path, rows: list[tuple[str, str]]) -> None: ...
def _write_bank_xlsx(path: Path, columns: list[str]) -> None: ...
```

From tests/test_test_de_eje_phase9_render_contract.py (render test pattern):
```python
class _FakeHTML:
    def __init__(self, string: str, base_url: str | None = None): ...
    def write_pdf(self) -> bytes: return b"%PDF-1.4\nphase9"
monkeypatch.setattr("reports.test_de_eje.generator.HTML", _FakeHTML)
```

PDU% thresholds (LOCKED from CONTEXT.md):
- PDU% < 50%         → ("Riesgo", "RR")
- 50% <= PDU% < 80%  → ("En desarrollo", "RD")
- PDU% >= 80%        → ("Solido", "RS")

Bank required columns for examen_de_eje: {pregunta, alternativa, unidad}  (NO leccion)
REGISTRY key expected: "examen_de_eje"
Generator class expected: ExamenDeEjeGenerator
Threshold function expected: _assign_estado_recomendacion(pdu_percent: float) -> tuple[str, str]
Unit dataclass expected: UnitStats (with .name, .total, .correct, .percent property)
Plan dataclass expected: ExamenPlan (with .units dict, .unit_order list)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing PDU% logic tests</name>
  <files>tests/test_examen_de_eje_phase11_logic.py</files>
  <behavior>
    - test_pdu_threshold_riesgo: _assign_estado_recomendacion(0.0) == ("Riesgo", "RR"), _assign_estado_recomendacion(49.9) == ("Riesgo", "RR")
    - test_pdu_threshold_boundary_50: _assign_estado_recomendacion(50.0) == ("En desarrollo", "RD")
    - test_pdu_threshold_en_desarrollo: _assign_estado_recomendacion(79.9) == ("En desarrollo", "RD")
    - test_pdu_threshold_boundary_80: _assign_estado_recomendacion(80.0) == ("Solido", "RS")
    - test_pdu_threshold_solido: _assign_estado_recomendacion(100.0) == ("Solido", "RS")
    - test_unit_stats_percent_with_zero_total: UnitStats(name="X").percent == 0.0 (no ZeroDivisionError)
    - test_unit_stats_percent_calculation: UnitStats with total=5, correct=4 → percent == 80.0
    - test_analyze_preserves_bank_row_order: given bank rows [Matematica financiera, Logaritmos, Numeros reales], analyze() returns unit_order matching that sequence (not alphabetical)
  </behavior>
  <action>
    Create tests/test_examen_de_eje_phase11_logic.py importing from reports.examen_de_eje.generator (which does not yet exist — tests must fail with ImportError on first run).

    Import: _assign_estado_recomendacion, UnitStats from reports.examen_de_eje.generator.

    Write 8 test functions matching the behavior list above. Use direct function calls and dataclass instantiation — no mocking needed for this file.

    For test_analyze_preserves_bank_row_order: build a minimal ExamenPlan with .unit_order = ["Matematica financiera", "Logaritmos", "Numeros reales"] (set manually as fixture, since the actual analyze() method needs the generator). Assert the list equals the expected order.
  </action>
  <verify>
    <automated>python -m pytest tests/test_examen_de_eje_phase11_logic.py --tb=short -q 2>&1 | head -20</automated>
  </verify>
  <done>File exists; running pytest shows ImportError or collection errors (RED state confirmed — no implementation yet). All 8 test functions are present in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing data contract and REGISTRY tests</name>
  <files>tests/test_examen_de_eje_phase11_data_contract.py</files>
  <behavior>
    - test_registry_contains_examen_de_eje_plugin: "examen_de_eje" in REGISTRY and REGISTRY["examen_de_eje"].__name__ == "ExamenDeEjeGenerator"
    - test_bank_columns_accepted_with_required_set: bank with [pregunta, alternativa, unidad] passes column check (no error raised)
    - test_bank_missing_column_raises_with_name: bank with [pregunta, alternativa] (missing unidad) raises ValueError mentioning "unidad"
    - test_bank_extra_column_allowed: bank with [pregunta, alternativa, unidad, extra_col] does not raise
    - test_load_mapping_accepts_valid_examen_de_eje_rows: ids.xlsx with "M30M2-EXAMEN DE EJE 1-DATA" + valid hex ID + matching bank file → mapping list has 1 entry with assessment_number=1, assessment_type="M30M2"
    - test_load_mapping_skips_test_de_eje_rows: ids.xlsx with "M30M2-TEST DE EJE 1-DATA" → mapping list is empty (regex mismatch)
    - test_load_mapping_raises_when_no_valid_rows: ids.xlsx with only junk rows → raises ValueError
  </behavior>
  <action>
    Create tests/test_examen_de_eje_phase11_data_contract.py.

    Helpers to include (mirror pattern from test_test_de_eje_phase9_data_contract.py):
    - _workdir(prefix): creates .tmp_testdata/{prefix}_{uuid4().hex}/ and returns Path
    - _write_ids_xlsx(path, rows): writes xlsx with header row ["assessment_name", "assessment_id"] + data rows
    - _write_bank_xlsx(path, columns): writes xlsx with one data row using the provided column names

    Import: REGISTRY from reports; ExamenDeEjeGenerator from reports.examen_de_eje.generator.

    For test_load_mapping_accepts_valid_examen_de_eje_rows: monkeypatch IDS_LOCAL_PATH and BANKS_DIR on the generator module to use tmp dirs.

    For the bank column validation tests (test_bank_columns_accepted and test_bank_missing_column): call gen.analyze() with a minimal download result dict that contains one DataFrame loaded from the synthetic bank, OR call a private helper if available. If analyze() is the integration point, write a small inline helper that builds a DataFrame with the given columns and one row, then call gen.analyze() with a synthetic student DataFrame. This is acceptable; the important thing is the behavior is tested at the public API level (analyze raises on missing columns).

    Use `from uuid import uuid4` and `from pathlib import Path` and `import pandas as pd` and `from openpyxl import Workbook`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_examen_de_eje_phase11_data_contract.py --tb=short -q 2>&1 | head -20</automated>
  </verify>
  <done>File exists; pytest shows ImportError or collection errors (RED state). All 7 test functions are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing render contract tests</name>
  <files>tests/test_examen_de_eje_phase11_render_contract.py</files>
  <behavior>
    - test_render_writes_pdf_with_correct_filename: render() for ExamenPlan with assessment_name="M30M2-EXAMEN DE EJE 1-DATA", email="student@example.com" creates file "M30M2-EXAMEN DE EJE 1__student@example.com.pdf" in output_dir
    - test_render_inserts_unit_status_rows_in_html: captured HTML from _FakeHTML contains "<tr>" and unit name strings from the test plan
    - test_render_output_dir_is_data_examen_de_eje_output: output_dir returned equals gen.data_dir / "output"
    - test_render_skips_plan_with_no_units: render() with an ExamenPlan that has empty .units and .unit_order produces no PDF file (no crash)
  </behavior>
  <action>
    Create tests/test_examen_de_eje_phase11_render_contract.py.

    Use _FakeHTML pattern from test_test_de_eje_phase9_render_contract.py:
    ```python
    class _FakeHTML:
        def __init__(self, string: str, base_url: str | None = None):
            self._string = string
        def write_pdf(self) -> bytes:
            captured_html.append(self._string)
            return b"%PDF-1.4\nphase11"
    monkeypatch.setattr("reports.examen_de_eje.generator.HTML", _FakeHTML)
    ```

    Build ExamenPlan fixtures directly (no download/analyze needed):
    - Create ExamenPlan with assessment_type="M30M2", student_id="u-1", email="student@example.com", assessment_name="M30M2-EXAMEN DE EJE 1-DATA"
    - Add 3 UnitStats entries (Matematica financiera, Logaritmos, Numeros reales) with total/correct values
    - Set unit_order list accordingly

    Monkeypatch gen.data_dir to a tmp path.

    For test_render_inserts_unit_status_rows_in_html: assert "Matematica financiera" in captured HTML and "<tr>" in captured HTML.

    For test_render_skips_plan_with_no_units: ExamenPlan with .units={} and .unit_order=[] → render() returns output_dir without error, no PDF created.
  </action>
  <verify>
    <automated>python -m pytest tests/test_examen_de_eje_phase11_render_contract.py --tb=short -q 2>&1 | head -20</automated>
  </verify>
  <done>File exists; pytest shows ImportError or collection errors (RED state). All 4 test functions are present.</done>
</task>

</tasks>

<verification>
Run all three test files together after writing them. All should fail due to missing implementation:

```bash
python -m pytest tests/test_examen_de_eje_phase11_logic.py tests/test_examen_de_eje_phase11_data_contract.py tests/test_examen_de_eje_phase11_render_contract.py --tb=line -q
```

Expected: ImportError on `reports.examen_de_eje.generator` — confirms RED state.
</verification>

<success_criteria>
- 3 test files created under tests/
- Combined 19 test functions present across the three files
- All tests fail with ImportError (no implementation exists yet)
- Test logic exactly matches the behavioral contracts locked in CONTEXT.md (PDU% thresholds at 50 and 80)
- No test imports from test_de_eje generator — each test is self-contained for examen_de_eje
</success_criteria>

<output>
After completion, create `.planning/phases/11-examen-de-eje-plugin/11-01-SUMMARY.md` covering:
- Which test functions were created and what behavioral contract each covers
- Confirmed RED state output snippet
- Any deviations from the plan (e.g., if analyze() column validation is tested differently)
</output>

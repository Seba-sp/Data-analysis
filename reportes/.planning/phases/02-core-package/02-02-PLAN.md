---
phase: 02-core-package
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - core/assessment_downloader.py
autonomous: true
requirements:
  - CORE-02

must_haves:
  truths:
    - "`from core.assessment_downloader import AssessmentDownloader` works from the repo root"
    - "The assembled file contains all methods listed in MERGE-DECISIONS.md Section 2 with `Destination = core/` — count must be >= 36 methods"
    - "No bare flat imports remain inside `core/assessment_downloader.py` itself (e.g., `from storage import` must not appear)"
    - "`GOOGLE_CLOUD_PROJECT` does not appear in the file — all occurrences replaced with `GCP_PROJECT_ID`"
    - "`cleanup_incremental_files` does not appear — canonical name is `cleanup_temp_files`"
  artifacts:
    - path: "core/assessment_downloader.py"
      provides: "Canonical merged AssessmentDownloader with all methods from all 6 copies"
      exports: ["AssessmentDownloader"]
      min_lines: 400
  key_links:
    - from: "core/assessment_downloader.py"
      to: "core/storage.py"
      via: "from core.storage import StorageClient"
      pattern: "from core\\.storage import StorageClient"
---

<objective>
Assemble `core/assessment_downloader.py` — the canonical single version of AssessmentDownloader — by merging all method versions per MERGE-DECISIONS.md Section 2 resolution decisions.

Purpose: This is the primary shared service. Every report plugin will use it for LearnWorlds API downloads. It must be complete (all 36+ methods), correct (per merge decisions), and clean (no bare imports, correct env var name).

Output: `core/assessment_downloader.py` — a fully assembled, import-clean AssessmentDownloader that replaces all 6 diverged copies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md
@.planning/phases/02-core-package/02-CONTEXT.md
@.planning/phases/02-core-package/02-RESEARCH.md
@.planning/phases/02-core-package/02-01-SUMMARY.md

<interfaces>
<!-- Source files to read during assembly. Executor reads all four, then builds the merged file. -->

Primary base (use for all DIVERGES methods unless table says otherwise):
- `diagnosticos/assessment_downloader.py` — canonical base for most methods

Secondary sources (use for specific methods as per MERGE-DECISIONS.md Section 2):
- `ensayos_generales/assessment_downloader.py` — base for: save_responses_to_csv, save_temp_responses_to_csv, cleanup_temp_files, add_answer_columns_to_csv, _download_and_process_common, download_users, merge_incremental_users, load_users_from_json, get_username_by_user_id, get_temp_csv_file_path, get_temp_analysis_file_path, get_latest_user_timestamp_from_json
- `assessment-analysis-project/assessment_downloader.py` — base for: download_form_responses_incremental, save_form_responses_to_csv, download_and_process_form, get_users_csv_file_path
- `diagnosticos/assessment_downloader.py` — also base for: filter_responses (use userId OR user_id fallback from diag)

IDENTICAL methods (copy verbatim from diagnosticos):
- get_json_file_path, get_csv_file_path, get_incremental_json_file_path
- _download_assessment_responses_full, save_responses_to_json, save_incremental_responses_to_json
- load_responses_from_json, delete_assessment_data, get_assessment_info

Key resolution decisions:
- `get_latest_timestamp_from_json`: use diag version (reads submittedTimestamp — NOT `created`). The aa version is a bug.
- `_download_responses_incremental`: use diag as base (submittedTimestamp + MIN_DOWNLOAD_DATE filter)
- `download_assessment_responses_incremental`: use diag as base (has return_df param)
- `save_responses_to_csv`: use eg as base (has include_usernames param + "No respondida" for empty)
- `_download_and_process_common`: use eg as base (writes temp CSV for incremental, more robust)
- `download_and_process_assessment` + `download_all_assessments`: use diag as base (has incremental_mode)
- `_download_form_responses_full`: copy verbatim from eg or aa (identical bodies)
- `cleanup_temp_files` (canonical name): use eg body (superset — deletes JSON + temp CSV + temp analysis)
- `filter_responses`: use diag version (handles both userId and user_id API variants)
- `__init__`: use diag as base (MIN_DOWNLOAD_DATE filter, min_date/min_timestamp attributes)

Per-report methods NOT in core/ (confirm absent from assembled file):
- `_normalize_commune`, `_compare_emails`, `_process_email_columns` → reports/assessment_analysis/ only
- `load_assessment_list_from_env`, `parse_arguments`, `main` → per-report modules only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all four source files and assemble core/assessment_downloader.py</name>
  <files>core/assessment_downloader.py</files>
  <action>
Read the four source files in full, then write `core/assessment_downloader.py` as the merged canonical version.

**Step 1 — Read all four source files:**
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/assessment_downloader.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/ensayos_generales/assessment_downloader.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/assessment-analysis-project/assessment_downloader.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos_uim/assessment_downloader.py`

**Step 2 — Assemble following ALL resolution decisions from MERGE-DECISIONS.md Section 2.**

**File header:**
```python
#!/usr/bin/env python3
"""
AssessmentDownloader — Canonical unified version.
Merged from 6 project copies per MERGE-DECISIONS.md (2026-02-28).
Canonical base: diagnosticos/ with additions from ensayos_generales/ and assessment-analysis-project/.
"""
```

**Imports — CRITICAL rules:**
- Change `from storage import StorageClient` → `from core.storage import StorageClient`
- All other imports copy verbatim from the source files (requests, json, pandas, pathlib, etc.)
- Do NOT add `from assessment_downloader import` or any other bare import
- The module-level `load_dotenv()` call: include it (present in diag source)

**Method assembly order** (follow MERGE-DECISIONS.md Section 2 table row order):
1. `__init__` — diag base (MIN_DOWNLOAD_DATE, min_timestamp, min_date)
2. `get_json_file_path` — verbatim from diag
3. `get_csv_file_path` — verbatim from diag
4. `get_incremental_json_file_path` — verbatim from diag
5. `get_temp_csv_file_path` — from eg (absent in diag)
6. `get_temp_analysis_file_path` — from eg (absent in diag)
7. `get_users_csv_file_path` — from aa (absent in diag/eg)
8. `get_latest_timestamp_from_json` — from diag (submittedTimestamp, NOT created)
9. `get_latest_user_timestamp_from_json` — from eg (absent in diag)
10. `_download_responses_incremental` — diag base (submittedTimestamp + MIN_DOWNLOAD_DATE)
11. `download_assessment_responses_incremental` — diag base (has return_df param)
12. `get_only_new_responses` — from diag
13. `download_form_responses_incremental` — from aa (absent in diag/eg)
14. `_download_responses_full` — diag base (MIN_DOWNLOAD_DATE filter)
15. `_download_assessment_responses_full` — verbatim from diag (identical)
16. `_download_form_responses_full` — from eg or aa (identical: single-line wrapper)
17. `save_responses_to_json` — verbatim from diag (identical)
18. `save_incremental_responses_to_json` — verbatim from diag (identical)
19. `save_temp_responses_to_csv` — from eg (absent in diag)
20. `save_responses_to_csv` — eg base (include_usernames param + "No respondida" for empty)
21. `save_form_responses_to_csv` — from aa (absent in diag/eg)
22. `merge_incremental_to_main_json` — verbatim from diag (identical)
23. `cleanup_temp_files` — eg body with canonical name `cleanup_temp_files` (NOT cleanup_incremental_files). eg body is superset: deletes incremental JSON + temp CSV + temp analysis files
24. `load_responses_from_json` — verbatim from diag (identical)
25. `filter_responses` — diag version (r.get('userId') or r.get('user_id') — handles both API variants; use Union type hint)
26. `add_answer_columns_to_csv` — eg base ("No respondida" for empty/NaN answers)
27. `delete_assessment_data` — verbatim from diag (identical)
28. `_download_and_process_common` — eg base (writes temp CSV for incremental path, not in-memory)
29. `download_and_process_form` — from aa (absent in diag/eg)
30. `download_and_process_assessment` — diag base (has incremental_mode param)
31. `download_all_assessments` — diag base (has incremental_mode param)
32. `get_assessment_info` — verbatim from diag (identical)
33. `download_users` — from eg (absent in diag)
34. `merge_incremental_users` — from eg (absent in diag)
35. `load_users_from_json` — from eg (absent in diag)
36. `get_username_by_user_id` — from eg (absent in diag)

**CRITICAL env var fix:** In the assembled `__init__` and any other method that reads `GOOGLE_CLOUD_PROJECT`, change all occurrences to `GCP_PROJECT_ID` per MERGE-DECISIONS.md Section 6 resolution.

**Do NOT include** in `core/assessment_downloader.py`:
- `_normalize_commune`, `_compare_emails`, `_process_email_columns` (aa domain-specific)
- `load_assessment_list_from_env`, `parse_arguments`, `main` (per-report module functions)
  </action>
  <verify>
    <automated>
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes" && python -c "
import ast, sys

# Parse the file for syntax errors
with open('core/assessment_downloader.py') as f:
    source = f.read()

try:
    tree = ast.parse(source)
    print('PASS: File parses without syntax errors')
except SyntaxError as e:
    print(f'FAIL: Syntax error: {e}')
    sys.exit(1)

# Count methods
methods = [node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)]
class_methods = [m for m in methods if m not in ('load_assessment_list_from_env', 'parse_arguments', 'main')]
print(f'INFO: Found {len(class_methods)} methods/functions in file')
assert len(class_methods) >= 36, f'FAIL: Expected >= 36 methods, got {len(class_methods)}'
print(f'PASS: Method count >= 36')

# Check no bare imports
import re
bare_imports = re.findall(r'^from storage import|^from assessment_downloader import|^from assessment_analyzer import|^from email_sender import|^from drive_service import', source, re.MULTILINE)
assert not bare_imports, f'FAIL: Bare imports found: {bare_imports}'
print('PASS: No bare flat imports')

# Check canonical env var name
assert 'GOOGLE_CLOUD_PROJECT' not in source, 'FAIL: GOOGLE_CLOUD_PROJECT still present, must be GCP_PROJECT_ID'
print('PASS: GCP_PROJECT_ID used (no GOOGLE_CLOUD_PROJECT)')

# Check canonical method name
assert 'cleanup_incremental_files' not in source, 'FAIL: cleanup_incremental_files must be cleanup_temp_files'
print('PASS: cleanup_temp_files used (no cleanup_incremental_files)')

# Check from core.storage import
assert 'from core.storage import StorageClient' in source, 'FAIL: Must import from core.storage'
print('PASS: from core.storage import StorageClient')

# Verify importable
from core.assessment_downloader import AssessmentDownloader
print('PASS: from core.assessment_downloader import AssessmentDownloader works')

print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - `core/assessment_downloader.py` exists and imports cleanly (`from core.assessment_downloader import AssessmentDownloader`)
    - File contains >= 36 methods (all entries from MERGE-DECISIONS.md Section 2 with Destination=core/)
    - No bare flat imports (`from storage import` etc.) anywhere in the file
    - `GCP_PROJECT_ID` used everywhere; `GOOGLE_CLOUD_PROJECT` absent
    - `cleanup_temp_files` is the method name; `cleanup_incremental_files` absent
    - `from core.storage import StorageClient` present in the imports section
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes"
python -c "from core.assessment_downloader import AssessmentDownloader; print('OK')"
grep -c "def " core/assessment_downloader.py
grep "from storage import\|from assessment_downloader import" core/assessment_downloader.py && echo "FAIL: bare imports" || echo "PASS: no bare imports"
grep "GOOGLE_CLOUD_PROJECT" core/assessment_downloader.py && echo "FAIL: old env var" || echo "PASS: env var clean"
```
</verification>

<success_criteria>
- `from core.assessment_downloader import AssessmentDownloader` succeeds from repo root
- `grep -c "def " core/assessment_downloader.py` returns >= 36
- No bare imports or legacy env var names in the file
- File is syntactically valid Python
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-package/02-02-SUMMARY.md`
</output>

---
phase: 14-gcp-and-gcs-webhook-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/runner.py
  - reports/base.py
  - reports/test_de_eje/generator.py
  - reports/examen_de_eje/generator.py
  - tests/test_phase14_filename_and_dedupe_contract.py
autonomous: true
requirements:
  - MAIL-01
  - PLUG-01
  - PLUG-02
  - ROUT-04
  - ROUT-05
  - DEPL-02
must_haves:
  truths:
    - "PDF filename contract is exactly informe_{report_type}_{assessment_name}_{email}.pdf for both plugins"
    - "Runner can parse report_type, assessment_name, and email from every generated filename"
    - "Sent-email dedupe key is exactly (report_type, assessment_name, email)"
    - "Dedupe only blocks exact triple duplicates and allows same email on different assessments"
    - "Successful SMTP sends append to data/{report_type}/processed_emails.xlsx only after send success"
    - "No ledger append is written when SMTP send fails"
  artifacts:
    - path: "core/runner.py"
      provides: "Filename parser + dedupe key + XLSX append-on-success path"
      contains: "processed_emails.xlsx"
    - path: "reports/test_de_eje/generator.py"
      provides: "Filename emission using locked contract"
      contains: "informe_"
    - path: "reports/examen_de_eje/generator.py"
      provides: "Filename emission using locked contract"
      contains: "informe_"
    - path: "tests/test_phase14_filename_and_dedupe_contract.py"
      provides: "Regression tests for contract parsing, dedupe, and ledger behavior"
      exports:
        - test_filename_contract_test_de_eje
        - test_filename_contract_examen_de_eje
        - test_runner_extracts_filename_contract
        - test_dedupe_key_is_report_assessment_email
        - test_processed_emails_xlsx_appends_only_after_success
  key_links:
    - from: "reports/*/generator.py render()"
      to: "core/runner.py filename parser"
      via: "informe_{report_type}_{assessment_name}_{email}.pdf"
      pattern: "^informe_"
    - from: "core/runner.py send loop"
      to: "data/{report_type}/processed_emails.xlsx"
      via: "append row after successful SMTP"
      pattern: "processed_emails.xlsx"
---

<objective>
Implement and lock delivery contracts that currently cause silent failures and duplicate-handling drift:
1) PDF filename contract = informe_{report_type}_{assessment_name}_{email}.pdf
2) sent-email dedupe key = (report_type, assessment_name, email)
3) processed-emails ledger = data/{report_type}/processed_emails.xlsx, append only on successful SMTP send

Purpose: make email delivery deterministic and auditable for one-assessment-per-email behavior.
Output: contract-compliant generators + runner parsing/dedupe/ledger logic + regression tests.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-CONTEXT.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-RESEARCH.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-VALIDATION.md
</execution_context>

<context>
Locked decisions for this plan:
- Filename is no longer the legacy informe_{email}_{assessment_type}.pdf.
- Dedupe identity must include assessment_name to avoid false skips.
- XLSX ledger is mandatory output and writes only after SMTP success.

Compatibility notes:
- Preserve existing CSV tracking only if already used by operational tooling; XLSX becomes source of truth.
- Keep backward-safe logging fields but do not preserve old filename assumptions.
</context>

<tasks>

<task id="14-01-01" type="auto" tdd="true">
  <name>Create RED tests for locked filename and dedupe/ledger contracts</name>
  <files>tests/test_phase14_filename_and_dedupe_contract.py</files>
  <action>
    Add failing tests that enforce the new locked contracts:
    - both generators emit informe_{report_type}_{assessment_name}_{email}.pdf
    - runner parser extracts exactly report_type, assessment_name, email from filename
    - dedupe key equality/inequality is based on (report_type, assessment_name, email)
    - processed_emails.xlsx row append occurs only after successful SMTP path
  </action>
  <verify>
    <automated>pytest tests/test_phase14_filename_and_dedupe_contract.py -x -q</automated>
  </verify>
  <done>At least one test is RED on pre-change code, proving contracts are not yet enforced.</done>
</task>

<task id="14-01-02" type="auto" tdd="true">
  <name>Implement locked filename contract in both generators</name>
  <files>reports/test_de_eje/generator.py, reports/examen_de_eje/generator.py</files>
  <action>
    In each generator render loop, replace legacy filename composition with:
    informe_{safe(report_type)}_{safe(assessment_name)}_{safe(email)}.pdf

    Use canonical assessment_name value (the assessment label from ids mapping, not assessment_type shorthand).
    Keep safe filename sanitization helper.
  </action>
  <verify>
    <automated>pytest tests/test_phase14_filename_and_dedupe_contract.py -k "filename_contract or extracts_filename" -q</automated>
  </verify>
  <done>Both generators emit contract-compliant filenames and parser tests are GREEN.</done>
</task>

<task id="14-01-03" type="auto" tdd="true">
  <name>Implement runner parser + dedupe triple + XLSX append-on-success</name>
  <files>core/runner.py, reports/base.py</files>
  <action>
    Update runner filename parsing logic to consume exactly 4-part stem pattern:
    informe_{report_type}_{assessment_name}_{email}

    Build sent-email dedupe key strictly as tuple(report_type, assessment_name, email).
    Ensure lookup and write paths use this triple consistently.

    Add/confirm XLSX ledger path data/{report_type}/processed_emails.xlsx.
    Append row schema minimum:
    report_type, assessment_name, email, attachment_filename, sent_at_utc, event_key

    Write append operation immediately after successful SMTP send; do not append on failure/exception paths.
  </action>
  <verify>
    <automated>pytest tests/test_phase14_filename_and_dedupe_contract.py -q</automated>
  </verify>
  <done>All new contract tests pass; no append is observed for failed sends.</done>
</task>

<task id="14-01-04" type="auto">
  <name>Run regression slice for existing plugin and delivery behavior</name>
  <files>tests/</files>
  <action>
    Execute targeted regression suite for test_de_eje/examen_de_eje + runner + email flow.
    Fix any breakages caused by filename parser or dedupe migration.
  </action>
  <verify>
    <automated>pytest tests/ -k "phase9 or phase11 or runner or email" -q</automated>
  </verify>
  <done>No regressions in existing phase 9/11 behavior with new contracts enabled.</done>
</task>

</tasks>

<verification>
Verification criteria:
- Contract tests pass and explicitly assert the three locked decisions.
- At least one SMTP failure test confirms no XLSX append side effect.
- Logs/errors remain actionable when filename parsing fails for malformed files.
</verification>

<success_criteria>
1. Filename contract is enforced at generation and parsing boundaries.
2. Dedupe key is exactly (report_type, assessment_name, email).
3. Per-report XLSX ledger is append-only on successful sends.
4. Existing report plugin regression tests remain green.
</success_criteria>

<output>
After execution, create `.planning/phases/14-gcp-and-gcs-webhook-fixes/14-01-SUMMARY.md` with evidence.
</output>
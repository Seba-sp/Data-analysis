---
phase: 14-gcp-and-gcs-webhook-fixes
plan: 03
type: tdd
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - core/runner.py
  - reports/test_de_eje/generator.py
  - reports/examen_de_eje/generator.py
  - core/batch_processor.py
  - tests/test_phase14_end_to_end_assessment_scoping.py
  - .planning/phases/14-gcp-and-gcs-webhook-fixes/14-VERIFICATION.md
autonomous: true
requirements:
  - MAIL-01
  - ROUT-04
  - ROUT-05
  - PLUG-01
  - PLUG-02
  - DEPL-02
must_haves:
  truths:
    - "PipelineRunner passes assessment_name through execution path so generators download only targeted assessment"
    - "Webhook event for assessment A never triggers download/send for assessment B within same report_type"
    - "Filename, dedupe, and XLSX ledger contracts remain enforced under real batch flow"
    - "Batch metrics and logs are assessment-scoped and actionable"
    - "Operational GCS sync checklist for ids.xlsx is documented in verification evidence"
  artifacts:
    - path: "core/runner.py"
      provides: "assessment_name pass-through into generator execution"
      contains: "assessment_name"
    - path: "reports/test_de_eje/generator.py"
      provides: "assessment_name-filtered download path"
      contains: "assessment_name"
    - path: "reports/examen_de_eje/generator.py"
      provides: "assessment_name-filtered download path"
      contains: "assessment_name"
    - path: "tests/test_phase14_end_to_end_assessment_scoping.py"
      provides: "E2E-ish protection for per-assessment processing and contracts"
      exports:
        - test_runner_passes_assessment_name_to_generator
        - test_batch_event_assessment_scope_isolated
        - test_same_email_different_assessments_are_not_deduped
        - test_successful_send_appends_xlsx_once
    - path: ".planning/phases/14-gcp-and-gcs-webhook-fixes/14-VERIFICATION.md"
      provides: "Phase evidence including Cloud/ops checks"
      contains: "ids.xlsx"
  key_links:
    - from: "BatchProcessor grouped assessment_name"
      to: "PipelineRunner(assessment_name=...)"
      via: "assessment-scoped execution"
      pattern: "assessment_name="
    - from: "PipelineRunner send success"
      to: "data/{report_type}/processed_emails.xlsx"
      via: "append row after SMTP success"
      pattern: "processed_emails.xlsx"
---

<objective>
Finalize Phase 14 by enforcing assessment-scoped execution end-to-end and validating locked contracts under integrated flow.

Purpose: ensure webhook -> queue -> batch -> runner -> generator -> email behaves correctly in local + deployment-oriented checks.
Output: integration-hardening changes, E2E-focused tests, and refreshed verification evidence.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-CONTEXT.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-RESEARCH.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-VALIDATION.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-01-PLAN.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-02-PLAN.md
</execution_context>

<context>
Execution focus after plans 01/02:
- confirm generators honor assessment_name filter during download/data acquisition
- ensure runner receives and forwards assessment_name from batch path
- verify no false dedupe between same email and different assessments
- produce deployment-oriented evidence aligned to DEPL-02 operational expectations
</context>

<tasks>

<task id="14-03-01" type="auto" tdd="true">
  <name>Create RED integration tests for assessment-scoped execution</name>
  <files>tests/test_phase14_end_to_end_assessment_scoping.py</files>
  <action>
    Add failing tests that assert:
    - runner forwards assessment_name into generator execution
    - batch invocation for assessment A does not process/send assessment B
    - dedupe permits same email with different assessment_name values
    - processed_emails.xlsx append count increments only on successful sends
  </action>
  <verify>
    <automated>pytest tests/test_phase14_end_to_end_assessment_scoping.py -x -q</automated>
  </verify>
  <done>RED tests expose any missing pass-through or scoping leaks.</done>
</task>

<task id="14-03-02" type="auto" tdd="true">
  <name>Implement runner/generator assessment_name pass-through and filter logic</name>
  <files>core/runner.py, reports/test_de_eje/generator.py, reports/examen_de_eje/generator.py</files>
  <action>
    Ensure PipelineRunner accepts assessment_name input and passes it into generator.generate/download path.
    In each generator, when assessment_name is non-empty, filter ids/API rows to exact normalized assessment_name target.
    Maintain legacy behavior when assessment_name is empty (fallback path for old queue records).
  </action>
  <verify>
    <automated>pytest tests/test_phase14_end_to_end_assessment_scoping.py -k "passes_assessment_name or scope_isolated" -q</automated>
  </verify>
  <done>Generator download paths are assessment-targeted and tests are GREEN.</done>
</task>

<task id="14-03-03" type="auto">
  <name>Run full Phase 14 regression + refresh verification evidence</name>
  <files>tests/, .planning/phases/14-gcp-and-gcs-webhook-fixes/14-VERIFICATION.md</files>
  <action>
    Run complete targeted suite for phase 14 plus impacted prior-phase tests.
    Refresh 14-VERIFICATION.md with:
    - automated test evidence
    - key log signatures (startup summary, per-assessment batch processing)
    - operational checklist for ids.xlsx GCS sync parity confirmation
  </action>
  <verify>
    <automated>pytest tests/ -k "phase14 or phase9 or phase11 or runner or webhook" -q</automated>
    <manual>
      1. Confirm local inputs/ids.xlsx and gs://data-analysis-465905-t6-mapping/ids.xlsx are aligned before deploy.
      2. Confirm Cloud Run startup logs include rejected_names summary line.
      3. Confirm at least one webhook event yields one assessment-scoped email with XLSX ledger row appended.
    </manual>
  </verify>
  <done>Verification doc updated with concrete pass/fail evidence and remaining gaps explicitly listed.</done>
</task>

</tasks>

<verification>
Verification criteria:
- All Phase 14 test files pass.
- Same-email different-assessment scenario proves no false dedupe.
- XLSX ledger append side effect occurs exactly on successful SMTP sends.
- Assessment-scoped processing is visible in batch logs and verification notes.
</verification>

<success_criteria>
1. End-to-end path is assessment-scoped with no cross-assessment leakage.
2. Locked filename/dedupe/ledger contracts hold under integrated flow.
3. PLUG-01 and PLUG-02 plugin paths remain stable with no regressions.
4. DEPL-02-oriented operational checks are documented and executable.
</success_criteria>

<output>
After execution, create `.planning/phases/14-gcp-and-gcs-webhook-fixes/14-03-SUMMARY.md` and update `14-VERIFICATION.md` evidence.
</output>
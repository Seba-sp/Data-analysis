---
phase: 14-gcp-and-gcs-webhook-fixes
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/assessment_mapper.py
  - webhook_service.py
  - core/batch_processor.py
  - tests/test_phase14_mapper_and_webhook_routing.py
autonomous: true
requirements:
  - ROUT-04
  - ROUT-05
  - DEPL-02
  - MAIL-01
  - PLUG-01
  - PLUG-02
must_haves:
  truths:
    - "AssessmentMapper exposes route metadata with assessment_name for webhook queuing"
    - "Startup summary log always emits accepted/rejected totals and rejected assessment names"
    - "ROUT-04 validation remains enforced for unsupported groups"
    - "Webhook queues student records including assessment_name"
    - "BatchProcessor groups queue items by assessment_name and processes each group independently"
    - "Legacy queue records without assessment_name do not crash processing"
  artifacts:
    - path: "core/assessment_mapper.py"
      provides: "get_route_full + rejected_names startup observability"
      contains: "get_route_full"
    - path: "webhook_service.py"
      provides: "assessment_name propagation into queued student payload"
      contains: "assessment_name"
    - path: "core/batch_processor.py"
      provides: "per-assessment grouping loop"
      contains: "group"
    - path: "tests/test_phase14_mapper_and_webhook_routing.py"
      provides: "Routing/queue grouping regression protection"
      exports:
        - test_get_route_full_returns_assessment_name
        - test_startup_summary_contains_rejected_names
        - test_webhook_queues_assessment_name
        - test_batch_groups_by_assessment_name
        - test_batch_legacy_records_without_assessment_name
  key_links:
    - from: "AssessmentMapper.get_route_full"
      to: "webhook_service.handle_webhook"
      via: "(report_type, assessment_type, assessment_name)"
      pattern: "assessment_name"
    - from: "webhook_service queue payload"
      to: "BatchProcessor.process_batch"
      via: "assessment_name field"
      pattern: "assessment_name"
---

<objective>
Rebuild routing and queue flow so each webhook event is scoped to the exact assessment that fired.
This plan covers mapper observability and per-assessment queue processing prerequisites for end-to-end correctness.

Purpose: stop cross-assessment contamination and make routing failures visible in production logs.
Output: mapper + webhook + batch changes with tests.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-CONTEXT.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-RESEARCH.md
@.planning/phases/14-gcp-and-gcs-webhook-fixes/14-VALIDATION.md
</execution_context>

<context>
Locked architecture decisions applied here:
- queue identity and grouping are assessment_name-aware
- mapper startup warnings must enumerate rejected names
- webhook signature validation stays unchanged

Group policy:
- Keep _ALLOWED_GROUPS aligned with current requirements and actual ids.xlsx policy.
- Explicitly document CL/L30M decision in code comment to prevent future ambiguity.
</context>

<tasks>

<task id="14-02-01" type="auto" tdd="true">
  <name>Create RED tests for mapper observability and route metadata</name>
  <files>tests/test_phase14_mapper_and_webhook_routing.py</files>
  <action>
    Add failing tests for:
    - get_route_full returns (report_type, assessment_type, assessment_name)
    - startup warning includes rejected_names list even when zero rows accepted
    - ROUT-04 unsupported groups still rejected and counted
  </action>
  <verify>
    <automated>pytest tests/test_phase14_mapper_and_webhook_routing.py -k "route_full or startup_summary or unsupported_groups" -x -q</automated>
  </verify>
  <done>RED tests prove mapper enhancements are required.</done>
</task>

<task id="14-02-02" type="auto" tdd="true">
  <name>Implement AssessmentMapper route_full and startup summary behavior</name>
  <files>core/assessment_mapper.py</files>
  <action>
    Implement/refresh mapper internals to persist assessment_name per normalized assessment_id.
    Add get_route_full() accessor without breaking existing get_route() return shape.

    Emit unconditional startup warning summary with:
    mapping_source, accepted, rejected, rejected_names.

    Preserve ROUT-04 group validation and finalize explicit _ALLOWED_GROUPS comment.
  </action>
  <verify>
    <automated>pytest tests/test_phase14_mapper_and_webhook_routing.py -k "route_full or startup_summary or unsupported_groups" -q</automated>
  </verify>
  <done>Mapper tests are GREEN and backward-compat get_route callers remain valid.</done>
</task>

<task id="14-02-03" type="auto" tdd="true">
  <name>Propagate assessment_name into webhook queue payload</name>
  <files>webhook_service.py</files>
  <action>
    Update webhook handler to use mapper route_full API, unpack assessment_name, and queue it with student payload.
    Keep existing error handling, signature validation, and core logging fields.
  </action>
  <verify>
    <automated>pytest tests/test_phase14_mapper_and_webhook_routing.py -k "webhook_queues_assessment_name" -q</automated>
  </verify>
  <done>Queued records include assessment_name for all routed webhook events.</done>
</task>

<task id="14-02-04" type="auto" tdd="true">
  <name>Group BatchProcessor work per assessment_name</name>
  <files>core/batch_processor.py</files>
  <action>
    Refactor process_batch to group queued students by assessment_name.
    Invoke processing path once per group and aggregate result metrics/errors.

    Legacy fallback:
    - missing assessment_name groups under empty string
    - no runtime crash or lost batch completion
  </action>
  <verify>
    <automated>pytest tests/test_phase14_mapper_and_webhook_routing.py -k "batch_groups_by_assessment_name or legacy_records_without_assessment_name" -q</automated>
  </verify>
  <done>Per-assessment grouping is active and tested.</done>
</task>

</tasks>

<verification>
Verification criteria:
- Mapper startup logs contain rejected_names in warning payload.
- Webhook tests confirm assessment_name is persisted in queue payload.
- Batch tests confirm one processing call per distinct assessment_name.
- No regression in signature validation behavior.
</verification>

<success_criteria>
1. ROUT-05 routing path now carries assessment_name to queue records.
2. ROUT-04 rejection behavior remains active and observable.
3. Batch execution is assessment-scoped and legacy-safe.
4. DEPL-02 operational diagnostics improve via startup warning summary.
</success_criteria>

<output>
After execution, create `.planning/phases/14-gcp-and-gcs-webhook-fixes/14-02-SUMMARY.md` with test/log evidence.
</output>
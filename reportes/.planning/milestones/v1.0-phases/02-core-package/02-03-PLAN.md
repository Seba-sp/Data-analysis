---
phase: 02-core-package
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - core/assessment_analyzer.py
autonomous: true
requirements:
  - CORE-03

must_haves:
  truths:
    - "`from core.assessment_analyzer import AssessmentAnalyzer` works from the repo root"
    - "Instantiating `AssessmentAnalyzer()` with no args uses the diagnosticos default config (contains M1, CL, CIEN, HYST keys)"
    - "No bare flat imports remain inside `core/assessment_analyzer.py` itself"
    - "`_analyze_by_category_generic` is present in the file (required for M1/CL/CIEN analysis)"
    - "The ensayos_generales analyzer is NOT included — it lives in reports/ensayos_generales/ (Phase 6)"
  artifacts:
    - path: "core/assessment_analyzer.py"
      provides: "Canonical merged AssessmentAnalyzer using diagnosticos as base for all methods"
      exports: ["AssessmentAnalyzer"]
      min_lines: 200
  key_links:
    - from: "core/assessment_analyzer.py"
      to: "core/storage.py"
      via: "from core.storage import StorageClient"
      pattern: "from core\\.storage import StorageClient"
---

<objective>
Assemble `core/assessment_analyzer.py` — the canonical single version of AssessmentAnalyzer — by merging all method versions per MERGE-DECISIONS.md Section 4 resolution decisions. Diagnosticos is the canonical base for every method.

Purpose: Used by all report plugins except ensayos_generales. The default config (M1/CL/CIEN/HYST assessment types) stays in core. Other report types (uim) pass their own config at instantiation.

Output: `core/assessment_analyzer.py` — complete, import-clean AssessmentAnalyzer.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md
@.planning/phases/02-core-package/02-CONTEXT.md
@.planning/phases/02-core-package/02-RESEARCH.md
@.planning/phases/02-core-package/02-01-SUMMARY.md

<interfaces>
<!-- Source files to read during assembly. Executor reads all three, then builds the merged file. -->

Primary base for ALL methods: `diagnosticos/assessment_analyzer.py`

Secondary sources (for specific methods per MERGE-DECISIONS.md Section 4):
- `diagnosticos_uim/assessment_analyzer.py` — reference only; uim is NOT the base for any method
- `assessment-analysis-project/assessment_analyzer.py` — reference only; aa is NOT the base for any method

Note: `ensayos_generales/assessment_analyzer.py` is architecturally SEPARATE — it has ZERO methods in common with the config-based family. Do NOT include any of its methods in `core/assessment_analyzer.py`.

Key resolution decisions (ALL use diagnosticos as base):
- `__init__`: diag base — full typed config dict, pass config=None to use defaults
- `_get_default_config`: diag base — full M1/CL/CIEN/HYST configs. uim's F30M/B30M/Q30M defaults stay in uim's report module
- `analyze_assessment`: diag base — raises ValueError for unknown assessment type (NOT uim's _default fallback)
- `_analyze_by_category_generic`: diag base — INCLUDE in core/ (intentionally absent from uim but needed for M1/CL/CIEN)
- `_analyze_percentage_based_generic`: diag base (superset of uim version)
- `_determine_level_unified`: diag base (handles all 4 types)
- `_get_internal_level`: diag base (handles M1/CL Nivel 4 distinction)
- `analyze_assessment_from_csv`: diag base (return_df param, StorageClient for I/O, accepts DataFrame input)
- IDENTICAL methods (copy verbatim from diag): _format_percentage_for_excel, _extract_answers_from_response, _analyze_by_lecture
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read source files and assemble core/assessment_analyzer.py</name>
  <files>core/assessment_analyzer.py</files>
  <action>
Read the three source files in full, then write `core/assessment_analyzer.py` as the merged canonical version.

**Step 1 — Read all three source files:**
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/assessment_analyzer.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos_uim/assessment_analyzer.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/assessment-analysis-project/assessment_analyzer.py`

Do NOT read `ensayos_generales/assessment_analyzer.py` — it is architecturally separate and goes into `reports/ensayos_generales/` (Phase 6).

**Step 2 — Assemble following ALL resolution decisions from MERGE-DECISIONS.md Section 4.**

**File header:**
```python
#!/usr/bin/env python3
"""
AssessmentAnalyzer — Canonical unified version.
Merged from config-based family (diagnosticos, diagnosticos_uim, assessment-analysis-project)
per MERGE-DECISIONS.md (2026-02-28). Canonical base: diagnosticos/ for all methods.

Default config: M1/CL/CIEN/HYST assessment types (diagnosticos configuration).
Other report types (diagnosticos_uim) pass their own config dict at instantiation.

NOTE: ensayos_generales uses a separate, architecturally distinct analyzer
(score-conversion table approach) that lives in reports/ensayos_generales/analyzer.py.
"""
```

**Imports — CRITICAL rules:**
- If `from storage import StorageClient` exists in source → change to `from core.storage import StorageClient`
- Copy all other imports verbatim from diag source (logging, pandas, typing, etc.)
- Do NOT add `from assessment_analyzer import` or any other bare module import

**Method assembly — ALL use diagnosticos as canonical base:**

1. `__init__(self, config: Dict[str, Any] = None)` — diag base. Accept optional config; fall back to `self._get_default_config()` if None.
2. `_get_default_config(self)` — diag base. Full config dict for M1 (difficulty_based), CL (skill_based), CIEN (materia_based), HYST (percentage_based). Do NOT include uim's F30M/B30M/Q30M defaults here — those belong in the uim report module.
3. `_format_percentage_for_excel(self, value)` — verbatim from diag (IDENTICAL across all)
4. `_extract_answers_from_response(self, response)` — verbatim from diag (IDENTICAL across all)
5. `analyze_assessment(self, assessment_name, responses)` — diag base. Raises `ValueError` for unknown assessment type (NOT the uim fallback-to-_default behavior).
6. `_analyze_by_category_generic(self, ...)` — diag version. MUST be included (intentionally absent from uim because uim assessments are percentage-only; core/ needs it for M1/CL/CIEN).
7. `_analyze_percentage_based_generic(self, ...)` — diag base (superset of uim version).
8. `_determine_level_unified(self, ...)` — diag base (handles all 4 assessment types).
9. `_analyze_by_lecture(self, ...)` — verbatim from diag (IDENTICAL across all).
10. `_get_internal_level(self, ...)` — diag base (M1/CL Nivel 4 distinction in `else` branch handles uim transparently).
11. `analyze_assessment_from_csv(self, ...)` — diag base: `return_df: bool = False` param, accepts DataFrame input, uses `StorageClient` for I/O (not bare `pd.read_csv`).

**Import fix inside `analyze_assessment_from_csv`**: If the diag source uses `StorageClient` and imports it at the top, ensure the import reads `from core.storage import StorageClient`.

**Do NOT include:**
- Any method from ensayos_generales/assessment_analyzer.py (`get_unique_identifiers`, `load_conversion_data`, `calculate_assessment_score`, `convert_score`, `analyze_all_assessments`)
- uim's F30M/B30M/Q30M assessment configs in `_get_default_config`
  </action>
  <verify>
    <automated>
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes" && python -c "
import ast, sys, re

# Parse for syntax errors
with open('core/assessment_analyzer.py') as f:
    source = f.read()

try:
    tree = ast.parse(source)
    print('PASS: File parses without syntax errors')
except SyntaxError as e:
    print(f'FAIL: Syntax error: {e}')
    sys.exit(1)

# Check _analyze_by_category_generic is present
assert '_analyze_by_category_generic' in source, 'FAIL: _analyze_by_category_generic missing'
print('PASS: _analyze_by_category_generic present')

# Check ensayos_generales methods are NOT present
eg_methods = ['get_unique_identifiers', 'load_conversion_data', 'calculate_assessment_score', 'convert_score', 'analyze_all_assessments']
for m in eg_methods:
    assert m not in source, f'FAIL: ensayos_generales method {m} must not be in core/assessment_analyzer.py'
print('PASS: No ensayos_generales methods in core/assessment_analyzer.py')

# Check no bare imports
bare = re.findall(r'^from storage import|^from assessment_analyzer import|^from assessment_downloader import', source, re.MULTILINE)
assert not bare, f'FAIL: Bare imports found: {bare}'
print('PASS: No bare flat imports')

# Verify import works
from core.assessment_analyzer import AssessmentAnalyzer
print('PASS: from core.assessment_analyzer import AssessmentAnalyzer works')

# Verify default config has M1/CL/CIEN
analyzer = AssessmentAnalyzer()
config = analyzer._get_default_config()
assert 'M1' in config.get('assessment_types', config), 'FAIL: M1 missing from default config'
assert 'CL' in config.get('assessment_types', config), 'FAIL: CL missing from default config'
assert 'CIEN' in config.get('assessment_types', config), 'FAIL: CIEN missing from default config'
print('PASS: Default config contains M1, CL, CIEN assessment types')

print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - `core/assessment_analyzer.py` exists and imports cleanly
    - `AssessmentAnalyzer()` instantiates with default config containing M1, CL, CIEN keys
    - `_analyze_by_category_generic` is present in the file
    - No ensayos_generales methods in the file
    - No bare flat imports anywhere in the file
    - `from core.storage import StorageClient` used (if StorageClient is imported)
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes"
python -c "from core.assessment_analyzer import AssessmentAnalyzer; a = AssessmentAnalyzer(); print('OK')"
grep "_analyze_by_category_generic" core/assessment_analyzer.py && echo "PASS: method present"
grep "from storage import\|from assessment_analyzer import" core/assessment_analyzer.py && echo "FAIL: bare imports" || echo "PASS: clean"
```
</verification>

<success_criteria>
- `from core.assessment_analyzer import AssessmentAnalyzer` succeeds from repo root
- `AssessmentAnalyzer()._get_default_config()` returns config with M1, CL, CIEN keys
- `_analyze_by_category_generic` method present in file
- No bare imports, no ensayos_generales methods
- File is syntactically valid Python
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-package/02-03-SUMMARY.md`
</output>

---
phase: 02-core-package
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - core/storage.py
  - core/email_sender.py
  - core/drive_service.py
  - core/slack_service.py
  - core/upload_folder_to_gcs.py
autonomous: true
requirements:
  - CORE-04

must_haves:
  truths:
    - "`from core.storage import StorageClient` works from the repo root"
    - "`from core.email_sender import EmailSender` works from the repo root"
    - "`from core.drive_service import DriveService` works from the repo root"
    - "`from core.slack_service import SlackService` works (or equivalent class name from the source)"
    - "`from core.upload_folder_to_gcs import *` works (module-level functions importable)"
    - "No bare flat imports remain inside any of the five promoted files"
    - "StorageClient uses `'gcs'` as the backend string check (NOT `'gcp'`) — matches the env var doc"
    - "The `shared/` directory no longer exists (deleted)"
  artifacts:
    - path: "core/storage.py"
      provides: "StorageClient — local/GCS backend switching"
      exports: ["StorageClient"]
    - path: "core/email_sender.py"
      provides: "EmailSender — SMTP email sending"
      exports: ["EmailSender"]
    - path: "core/drive_service.py"
      provides: "DriveService — Google Drive upload"
      exports: ["DriveService"]
    - path: "core/slack_service.py"
      provides: "Slack notification utility"
    - path: "core/upload_folder_to_gcs.py"
      provides: "GCS folder upload utility"
  key_links:
    - from: "core/email_sender.py"
      to: "core/storage.py"
      via: "from core.storage import StorageClient (if present)"
      pattern: "core\\.storage"
---

<objective>
Promote the five service files from `shared/` to `core/`, fixing all internal bare imports and correcting the `StorageClient` backend string bug. Then delete the `shared/` directory (dead code).

Purpose: These files are the infrastructure layer that AssessmentDownloader and future PipelineRunner depend on. They were copied from shared/ into each project as live copies — shared/ was never imported from directly. Promoting to core/ with correct imports makes them the single source of truth.

Output: Five files in `core/` with clean package imports. `shared/` deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-consolidation-audit/MERGE-DECISIONS.md
@.planning/phases/02-core-package/02-CONTEXT.md
@.planning/phases/02-core-package/02-RESEARCH.md
@.planning/phases/02-core-package/02-01-SUMMARY.md

<interfaces>
<!-- Source files for promotion. Per CONTEXT.md locked decision: copy as-is, fix imports only.
     The canonical source is the best copy per Phase 1 merge decisions.
     Since these files have no method-level divergence decisions in MERGE-DECISIONS.md,
     use the diagnosticos/complete_deployment/ copies as the production source of truth. -->

Source files (production copies — diagnosticos/complete_deployment/):
- `diagnosticos/complete_deployment/storage.py` — PRODUCTION canonical source
- `diagnosticos/complete_deployment/email_sender.py` — PRODUCTION canonical source
- `diagnosticos/complete_deployment/drive_service.py` — PRODUCTION canonical source

Source files (shared/ — for slack_service and upload_folder_to_gcs which are not in complete_deployment/):
- `shared/slack_service.py`
- `shared/upload_folder_to_gcs.py`

KNOWN BUG TO FIX in storage.py:
- `shared/storage.py` and `diagnosticos/complete_deployment/storage.py` both check `if self.backend == 'gcp':`
- BUT the env var documentation uses `STORAGE_BACKEND=gcs` (not `gcp`)
- Resolution: during promotion, verify the check string. If it reads `'gcp'`, change it to `'gcs'` to match the env var documentation. If it already reads `'gcs'`, keep it as-is.
- Also update `get_backend_info()` return value accordingly if it hardcodes `'gcp'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote storage.py, email_sender.py, drive_service.py from complete_deployment/</name>
  <files>core/storage.py, core/email_sender.py, core/drive_service.py</files>
  <action>
Read the three production source files and write them to `core/` with import fixes applied.

**Step 1 — Read the three production source files:**
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/complete_deployment/storage.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/complete_deployment/email_sender.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/diagnosticos/complete_deployment/drive_service.py`

**Step 2 — Write `core/storage.py`:**
Copy verbatim from the production source with these changes:
- Add file header comment: `# Promoted from diagnosticos/complete_deployment/storage.py — Phase 2`
- **Backend string bug fix:** Find the check `if self.backend == 'gcp':` and change it to `if self.backend == 'gcs':` to match `STORAGE_BACKEND=gcs` env var documentation. If the production copy already uses `'gcs'`, leave it unchanged.
- Also update any `get_backend_info()` references from `'gcp'` to `'gcs'` for consistency.
- No other changes.

**Step 3 — Write `core/email_sender.py`:**
Copy verbatim from the production source with these changes:
- Add file header comment: `# Promoted from diagnosticos/complete_deployment/email_sender.py — Phase 2`
- If any internal import reads `from storage import StorageClient` → change to `from core.storage import StorageClient`
- No other changes.

**Step 4 — Write `core/drive_service.py`:**
Copy verbatim from the production source with these changes:
- Add file header comment: `# Promoted from diagnosticos/complete_deployment/drive_service.py — Phase 2`
- If any internal import reads `from storage import StorageClient` → change to `from core.storage import StorageClient`
- No other changes.
  </action>
  <verify>
    <automated>
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes" && python -c "
import re, sys

# Verify all three files import cleanly
from core.storage import StorageClient
print('PASS: from core.storage import StorageClient')

from core.email_sender import EmailSender
print('PASS: from core.email_sender import EmailSender')

from core.drive_service import DriveService
print('PASS: from core.drive_service import DriveService')

# Verify storage.py uses 'gcs' not 'gcp'
with open('core/storage.py') as f:
    storage_src = f.read()

# Find backend check string
gcp_checks = re.findall(r\"== 'gcp'\", storage_src)
assert not gcp_checks, f\"FAIL: storage.py still uses 'gcp' backend string: {gcp_checks}\"
print(\"PASS: storage.py uses 'gcs' backend string (not 'gcp')\")

# Verify no bare imports in the promoted files
for fname in ['core/storage.py', 'core/email_sender.py', 'core/drive_service.py']:
    with open(fname) as f:
        src = f.read()
    bare = re.findall(r'^from storage import|^from email_sender import|^from drive_service import', src, re.MULTILINE)
    assert not bare, f'FAIL: Bare imports in {fname}: {bare}'
    print(f'PASS: No bare imports in {fname}')

print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - `core/storage.py`, `core/email_sender.py`, `core/drive_service.py` all import cleanly
    - `StorageClient` backend check uses `'gcs'` string (not `'gcp'`)
    - No bare flat imports inside any of the three files
  </done>
</task>

<task type="auto">
  <name>Task 2: Promote slack_service.py and upload_folder_to_gcs.py, then delete shared/</name>
  <files>core/slack_service.py, core/upload_folder_to_gcs.py</files>
  <action>
Read the two shared/ source files, promote them to core/, then delete the shared/ directory.

**Step 1 — Read both source files:**
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/shared/slack_service.py`
- `C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/shared/upload_folder_to_gcs.py`

**Step 2 — Write `core/slack_service.py`:**
Copy verbatim with these changes:
- Add file header comment: `# Promoted from shared/slack_service.py — Phase 2`
- If any internal import reads `from storage import` → change to `from core.storage import`
- No other changes (per CONTEXT.md locked decision: copy as-is, fix imports only)

**Step 3 — Write `core/upload_folder_to_gcs.py`:**
Copy verbatim with these changes:
- Add file header comment: `# Promoted from shared/upload_folder_to_gcs.py — Phase 2`
- If any internal import reads `from storage import` → change to `from core.storage import`
- No other changes (Claude's Discretion decision: promote as-is without refactoring)

**Step 4 — Delete the `shared/` directory:**
The `shared/` folder is confirmed dead code per CONTEXT.md (no report currently imports from it; each project has its own copy of these files). Delete it entirely.

Use Python to delete:
```python
import shutil
shutil.rmtree('C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/shared')
```

Or via bash:
```bash
rm -rf "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes/shared"
```
  </action>
  <verify>
    <automated>
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes" && python -c "
import re, sys, os

# Verify slack_service imports
import importlib.util
spec = importlib.util.spec_from_file_location('slack_service', 'core/slack_service.py')
mod = importlib.util.module_from_spec(spec)
# Don't actually execute (needs Slack credentials), just check syntax
import ast
with open('core/slack_service.py') as f:
    src = f.read()
ast.parse(src)
print('PASS: core/slack_service.py parses without syntax errors')

with open('core/upload_folder_to_gcs.py') as f:
    src2 = f.read()
ast.parse(src2)
print('PASS: core/upload_folder_to_gcs.py parses without syntax errors')

# Check no bare imports
for fname in ['core/slack_service.py', 'core/upload_folder_to_gcs.py']:
    with open(fname) as f:
        src = f.read()
    bare = re.findall(r'^from storage import|^from slack_service import', src, re.MULTILINE)
    assert not bare, f'FAIL: Bare imports in {fname}: {bare}'
    print(f'PASS: No bare imports in {fname}')

# Verify shared/ is deleted
assert not os.path.exists('shared'), 'FAIL: shared/ directory still exists — must be deleted'
print('PASS: shared/ directory deleted')

print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - `core/slack_service.py` exists and is syntactically valid
    - `core/upload_folder_to_gcs.py` exists and is syntactically valid
    - No bare flat imports in either file
    - `shared/` directory no longer exists
    - All five `core/` service files are in place: storage.py, email_sender.py, drive_service.py, slack_service.py, upload_folder_to_gcs.py
  </done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/Seba/Downloads/M30M/Data-analysis/reportes"
python -c "
from core.storage import StorageClient
from core.email_sender import EmailSender
from core.drive_service import DriveService
print('PASS: all three primary services import OK')
"
python -c "import os; assert not os.path.exists('shared'), 'FAIL: shared/ still exists'"
grep -r "== 'gcp'" core/storage.py && echo "FAIL: backend string bug" || echo "PASS: backend string clean"
grep -r "^from storage import\|^from email_sender import\|^from drive_service import" core/ && echo "FAIL: bare imports" || echo "PASS: no bare imports in core/"
```
</verification>

<success_criteria>
- `from core.storage import StorageClient`, `from core.email_sender import EmailSender`, `from core.drive_service import DriveService` all succeed
- `core/slack_service.py` and `core/upload_folder_to_gcs.py` exist and parse cleanly
- `StorageClient` backend check uses `'gcs'` string
- `shared/` directory does not exist
- No bare imports in any `core/` file
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-package/02-04-SUMMARY.md`
</output>
